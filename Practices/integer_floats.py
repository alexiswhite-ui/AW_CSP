'''price = float(input("How much did it cost: $"))

sales_tax = float(input ("How much is the sales tax: %"))/100


total = round(price + (price*sales_tex), 2)


print(total)'''


apples = int(input("How many apples are there:"))

friends = 8

print("each friends can have", apples//friends, "amd I have", apples%friends, "left over")